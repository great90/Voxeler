#pragma once

using vint = int;
using vfloat = float;
using vufloat = double;
using vlong = long;
using vshort = short;
using vchar = char;
using vstring = char*;
using vbool = bool;

typedef glm::vec3(Vector3)(glm::vec3);
typedef glm::fvec3(Vector3f)(glm::fvec3);